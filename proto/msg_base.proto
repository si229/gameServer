syntax = "proto3";
import "google/protobuf/empty.proto";

enum c_cmd{
    heart_beat_req   = 0;
    heart_beat_resp  = 1;
}

service msg_base_service{
    rpc heartbeat(heart_beat_req) returns (heart_beat_resp);
}

message heart_beat_req{

}
message heart_beat_resp{

}
